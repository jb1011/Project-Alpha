node_modules/
dist/
# Local runtime data (SQLite + generated operating-agreement docs)
data/
legalbody.db
legalbody.db-shm
legalbody.db-wal
documents/
.env
.env.*
!.env.example
!.env.sandbox.example
# Circle entity-secret recovery files. The DevC SDK writes recovery_file_<uuid>.dat into the
# working directory on registerEntitySecretCiphertext — it is the ONLY way to recover the
# entity secret, so keep it offline and never in the repo.
recovery_file_*.dat
test/fixtures/*.local.json
tools/passkey-capture/capture.js

# Coverage reports (vitest --coverage)
coverage/

# Real attestation response captured by the W7.2 probe — review before ever committing
test/world/fixtures/attest-verify-response.json
recovery/
