# @circle-fin/developer-controlled-wallets has an OPTIONAL Solana peer dep that conflicts with
# @circle-fin/x402-batching's Solana tree. We are EVM-only; legacy resolution is deliberate and
# must behave identically on dev machines, CI, and the VPS (whose deploy runs `npm install`).
legacy-peer-deps=true
